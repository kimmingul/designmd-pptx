# Public benchmark methodology (v1.0.0)

## Rights

- **License:** CC0-1.0
- **Content:** 100% synthetic deck-specs generated by `designmd-pptx.public_benchmark`.
- **No** customer, partner, or scraped decks. Private corpus admission remains
  documented in [corpus.md](corpus.md) and is out of band for this publication.

## What we measure

Each fixture is scored **before → after** with
`python/designmd_pptx/benchmark_thresholds.json`:

| Metric | Max allowed (after) |
|---|---|
| corruption | 0 |
| extraction_loss | 5 |
| layout_failure | 0 |
| visual_gate_failure | 0 |
| a11y_error | 0 |

**Before** side intentionally weakens contrast and strips image `alt`.
**After** side runs `a11y.auto_correct_contrast` + `ensure_notes_and_alt`.

## Corpus construction

- Target size: **≥ 100** decks (this run: **100**)
- Themes: airy-keynote, dark-contrast, default, dense-consulting, light
- Recipes covered (61): `agenda_toc`, `appendix_table`, `before_after_slider`, `big_number`, `bullets`, `business_canvas`, `calendar_heatmap`, `case_study_band`, `chart_callout_panel`, `chart_insight`, `chevron_process`, `close`, `comparison_2col`, `consort_flow`, `cover`, `cycle_loop`, `device_frame`, `feature_cards`, `finance_statement`, `fishbone_causes`, `forest_plot`, `framework_row`, `freeform`, `funnel_stages`, `gantt_bars`, `geo_callout`, `hub_spoke`, `iceberg_levels`, `icon_stat_row`, `image_full`, `image_text_2col`, `kaplan_meier`, `kpi_dashboard_grid`, `kpi_row`, `logo_strip`, `matrix_2x2`, `multi_panel_figure`, `okrs_tree`, `org_tree`, `persona_card`, … (+21 more)
- Each deck: cover + primary recipe + close (3 slides)
- Deterministic ids: `pub-NNN-{theme}-{recipe}`

## Results (this run)

| | |
|---|---|
| Status | **PASS** |
| Pass | 100 |
| Fail | 0 |
| Skip | 0 |
| Total | 100 |
| Pass rate | 100.0% |

### Failures

_None_

## How to reproduce

```bash
PYTHONPATH=python python -m designmd_pptx benchmark --public -o public-benchmark-out
# optional size override:
PYTHONPATH=python python -m designmd_pptx benchmark --public --public-n 120 -o out
```

Machine-readable artifacts: `public-benchmark.summary.json`,
`public-corpus.index.json`, `benchmark.report.json`.
