PyYAML>=6.0

# OfficeCLI is a separate native/npm toolchain, not a pip dependency. Its
# supported version range is pinned in designmd_pptx/compatibility.json
# (issue #8) — official agent-bridge >= 0.2.117 (pinned 0.2.117);
# legacy shape-level binary is verb-gated. Verify with `designmd_pptx doctor`.
